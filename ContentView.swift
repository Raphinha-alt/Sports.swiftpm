import SwiftUI

struct ContentView: View {
    var body: some View {
      Text("Welocme")
            .font(.largeTitle)
        Text("Select Your Sport")
        Text("(click the icon)")
        
        
  
    }
}
