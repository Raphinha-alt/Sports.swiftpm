import SwiftUI

struct ContentView: View {
    var body: some View {
      Text("Welcome")
            .font(.largeTitle)
        Text("Select Your Sport")
        Text("(click the icon)")
        
        
  
        Button("🏀") {
            
        }
        .font(.custom("basketball", fixedSize: 125))
    }
}
